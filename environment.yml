name: deepfake-detector
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=1.13.0
      - torchvision>=0.14.0
      - transformers>=4.35.0
      - datasets
      - scikit-learn
      - matplotlib
      - tqdm
      - opencv-python
      - seaborn
      - jupyter
      - pandas
      - peft>=0.8.0
      - pillow
      - sentencepiece
